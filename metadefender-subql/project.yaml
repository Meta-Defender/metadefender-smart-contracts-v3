specVersion: "1.0.0"

name: "test-acala-by-evm"
version: "0.0.1"
runner:
  node:
    name: "@subql/node-ethereum"
    version: "*"
  query:
    name: "@subql/query"
    version: "*"
description: "This project can be use as a starting point for developing your
  new Ethereum SubQuery project"
repository: "https://github.com/subquery/ethereum-subql-starter"

schema:
  file: "./schema.graphql"

network:
  # chainId is the EVM Chain ID, for Ethereum this is 1
  # https://chainlist.org/chain/1
  chainId: "787"
  # This endpoint must be a public non-pruned archive node
  # We recommend providing more than one endpoint for improved reliability, performance, and uptime
  # Public nodes may be rate limited, which can affect indexing speed
  # When developing your project we suggest getting a private API key
  # You can get them from OnFinality for free https://app.onfinality.io
  # https://documentation.onfinality.io/support/the-enhanced-api-service
  endpoint: https://eth-rpc-acala.aca-api.network
  # Recommended to provide the HTTP endpoint of a full chain dictionary to speed up processing
  dictionary: "https://gx.api.subquery.network/sq/subquery/eth-dictionary"

dataSources:
  - kind: ethereum/Runtime
    startBlock: 5600000
    options:
      # Must be a key of assets
      abi: MetaDefenderMarketRegistry
      address: "0x022EC7DA43b6DECD855D622c4d23Ec5b19f3b639" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      MetaDefenderMarketRegistry:
        file: artifacts/contracts/periphery/MetaDefenderMarketsRegistry.sol/MetaDefenderMarketsRegistry.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handleMarketAdded
          kind: ethereum/LogHandler
          filter:
            topics:
              - MarketAdded(address metaDefender,address liquidityCertificate,address policy,address epochManage,string marketName,string marketDescription,string marketPaymentToken,string marketProtectionType,string network,uint256 timestamp)
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      abi: MetaDefender
      address: "0xF3c96A533D686C08732de32936362E48E48f2527"
    assets:
      MetaDefender:
        file: artifacts/contracts/MetaDefender.sol/MetaDefender.json
    mapping:
        file: "./dist/index.js"
        handlers:
            - handler: handleNewPolicyBuyer
              kind: ethereum/LogHandler
              filter:
                topics:
                  - NewPolicyBought(uint256 policyId, address policyBuyer, uint256 amount);
            - handler: handleRewardsClaimed
              kind: ethereum/LogHandler
              filter:
                topics:
                  - RewardsClaimed(address provider, uint256 rewards);
            - handler: handleCoverageClaimedAndTransferred
              kind: ethereum/LogHandler
              filter:
                topics:
                  - CoverageClaimedAndTransferred(uint256 id, address policyBuyer, uint256 coverage);
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      abi: MetaDefender
      address: "0x1888F146A1b3eBE17C7Ac9e8764e19442cC29b55"
    assets:
      MetaDefender:
        file: artifacts/contracts/MetaDefender.sol/MetaDefender.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handleNewPolicyBuyer
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewPolicyBought(uint256 policyId, address policyBuyer, uint256 amount);
        - handler: handleRewardsClaimed
          kind: ethereum/LogHandler
          filter:
            topics:
              - RewardsClaimed(address provider, uint256 amount);
        - handler: handleCoverageClaimedAndTransferred
          kind: ethereum/LogHandler
          filter:
            topics:
              - CoverageClaimedAndTransferred(uint256 id, address policyBuyer, uint256 coverage);
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      abi: MetaDefender
      address: "0x397b739660FD21B75838ae16Dc90c026221E5030"
    assets:
      MetaDefender:
        file: artifacts/contracts/MetaDefender.sol/MetaDefender.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handleNewPolicyBuyer
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewPolicyBought(uint256 policyId, address policyBuyer, uint256 amount);
        - handler: handleRewardsClaimed
          kind: ethereum/LogHandler
          filter:
            topics:
              - RewardsClaimed(address provider, uint256 amount);
        - handler: handleCoverageClaimedAndTransferred
          kind: ethereum/LogHandler
          filter:
            topics:
              - CoverageClaimedAndTransferred(uint256 id, address policyBuyer, uint256 coverage);
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      # Must be a key of assets
      abi: LiquidityCertificate
      address: "0x00B1A65888012B4BdBbe8A5bdeFC336ebE37f29F" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      LiquidityCertificate:
        file: artifacts/contracts/LiquidityCertificate.sol/LiquidityCertificate.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handleLPTransfer
          kind: ethereum/LogHandler
          filter:
            topics:
              - Transfer(address indexed from,address indexed to,uint256 indexed tokenId)
        - handler: handleLPExpired
          kind: ethereum/LogHandler
          filter:
            topics:
              - Expired(uint256 certificateId)
        - handler: handleNewLPMinted_glimmer
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewLPMinted(address owner,uint256 certificateId,uint256 enteredEpochIndex,uint256 liquidity,address metadefender)
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      # Must be a key of assets
      abi: LiquidityCertificate
      address: "0x98CCAF0c07e448A9acA9484Ea72582eacfe4E7c4" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      LiquidityCertificate:
        file: artifacts/contracts/LiquidityCertificate.sol/LiquidityCertificate.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handleLPTransfer
          kind: ethereum/LogHandler
          filter:
            topics:
              - Transfer(address indexed from,address indexed to,uint256 indexed tokenId)
        - handler: handleLPExpired
          kind: ethereum/LogHandler
          filter:
            topics:
              - Expired(uint256 certificateId)
        - handler: handleNewLPMinted_flame
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewLPMinted(address owner,uint256 certificateId,uint256 enteredEpochIndex,uint256 liquidity,address metadefender)
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      # Must be a key of assets
      abi: LiquidityCertificate
      address: "0x60Da4F04A0D9872ed011C1a75e3B986f901988F8" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      LiquidityCertificate:
        file: artifacts/contracts/LiquidityCertificate.sol/LiquidityCertificate.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handleLPTransfer
          kind: ethereum/LogHandler
          filter:
            topics:
              - Transfer(address indexed from,address indexed to,uint256 indexed tokenId)
        - handler: handleLPExpired
          kind: ethereum/LogHandler
          filter:
            topics:
              - Expired(uint256 certificateId)
        - handler: handleNewLPMinted_blaze
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewLPMinted(address owner,uint256 certificateId,uint256 enteredEpochIndex,uint256 liquidity,address metadefender)
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      # Must be a key of assets
      abi: Policy
      address: "0x705D5A5F9Eb920a326e2D2e139Ed67F0929D86e7" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      Policy:
        file: artifacts/contracts/Policy.sol/Policy.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handlePolicyClaimed
          kind: ethereum/LogHandler
          filter:
            topics:
              - PolicyClaimed(uint256 policyId, bool status)
        - handler: handleNewPolicyMinted_glimmer
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewPolicyMinted(address beneficiary,uint256 policyId,uint256 timestamp,uint256 coverage,uint256 fee,uint256 duration,uint256 standardRisk,uint256 enteredEpochIndex,uint256 SPS,address protocol,address epochManage)
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      # Must be a key of assets
      abi: Policy
      address: "0x2e00823b36d3c52D1f9cb0E3d80EdbF9Aa83650A" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      Policy:
        file: artifacts/contracts/Policy.sol/Policy.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handlePolicyClaimed
          kind: ethereum/LogHandler
          filter:
            topics:
              - PolicyClaimed(uint256 policyId, bool status)
        - handler: handleNewPolicyMinted_flame
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewPolicyMinted(address beneficiary,uint256 policyId,uint256 timestamp,uint256 coverage,uint256 fee,uint256 duration,uint256 standardRisk,uint256 enteredEpochIndex,uint256 SPS,address protocol,address epochManage)
  - kind: ethereum/Runtime
    startBlock: 4770000
    options:
      # Must be a key of assets
      abi: Policy
      address: "0x1f6C311DEBdDa08803c1bc41dDf7CBEF5a348BE0" # this is the contract address for wrapped ether https://etherscan.io/address/0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2
    assets:
      Policy:
        file: artifacts/contracts/Policy.sol/Policy.json
    mapping:
      file: "./dist/index.js"
      handlers:
        - handler: handlePolicyClaimed
          kind: ethereum/LogHandler
          filter:
            topics:
              - PolicyClaimed(uint256 policyId, bool status)
        - handler: handleNewPolicyMinted_blaze
          kind: ethereum/LogHandler
          filter:
            topics:
              - NewPolicyMinted(address beneficiary,uint256 policyId,uint256 timestamp,uint256 coverage,uint256 fee,uint256 duration,uint256 standardRisk,uint256 enteredEpochIndex,uint256 SPS,address protocol,address epochManage)